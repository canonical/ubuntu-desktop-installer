name: ubuntu_test
description: Shared test extensions and mocks.
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  build_runner: ^2.1.1
  effective_dart: ^1.3.1
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  gsettings: ^0.2.0
  mockito: 5.0.14
  subiquity_client:
    path: ../subiquity_client
