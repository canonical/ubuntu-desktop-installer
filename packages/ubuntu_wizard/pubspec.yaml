name: ubuntu_wizard
description: Shared library for Ubuntu wizards.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  ubuntu_localizations: ^0.2.0
  wizard_router: ^1.0.1
  yaru: ^0.7.0
  yaru_colors: ^0.1.6
  yaru_widgets: ^2.4.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  ubuntu_test: ^0.1.0-0
  yaru_test: ^0.1.0
